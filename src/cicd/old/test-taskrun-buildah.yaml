apiVersion: tekton.dev/v1alpha1
kind: TaskRun
metadata:
  generateName: test-build-run-
spec:
  serviceAccountName: posinaga-github-gcr-5
  taskRef:
    # name: kaniko
    name: buildah
  inputs:
    resources:
    - name: source
      resourceSpec:
        type: git
        params:
        - name: url
          value: https://github.com/posinaga-demo/basictoy 
  outputs:
    resources:
    - name: image
      resourceSpec:
        type: image
        params:
        - name: url
          value: gcr.io/basictoy/pirulardi25
